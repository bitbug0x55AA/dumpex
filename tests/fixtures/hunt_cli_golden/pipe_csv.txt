## hunt / summary
ttp,score,max_score,status,verdict,verdict_level,confidence,coverage_complete,coverage_reason,lead_count,review_priority
pipe,3,3,DETECTED,HIGH,high,high,False,1 region(s) returned fewer bytes than declared (short read) — not fully scanned,1,high

## hunt / findings
ttp,finding_type,va_process,file_offset,cs_version,xor_key,beacon_type,c2_host,c2_uri,port,useragent,pipename,license_id,sleep_ms,jitter_pct,details
pipe,pipe_open_handle,,,,,,,,,,\Device\NamedPipe\msagent_1337,,,,Handle=0x88; framework=Cobalt Strike
pipe,pipe_string_scan_lead,0x0000000001230101,,,,,,,,,\.\pipe\msagent_1337,,,,PAGE_EXECUTE_READ

## hunt / finding_details
ttp,check,tag,confidence,inference,rationale,facts,limitations
pipe,pipe.open_handles,observation,medium,"Process holds 1 open handle(s) to named pipe object(s), per HandleDataStream.","HandleDataStream is the OS's own record of what this process actually has open — far stronger than a bare string match, but having ANY pipe handle open is completely ordinary for most Windows processes (IPC, RPC, print spooler, etc.); only a handle whose name matches a known C2/lateral-movement framework convention, or one corroborated by nearby C2 context / active execution, is treated as a detection.",Handle=0x88 ObjectName=\Device\NamedPipe\msagent_1337 GrantedAccess=0x12019f  [framework=Cobalt Strike],Presence of a pipe handle is not inherently suspicious.
pipe,pipe.handle_framework_match,detection,medium,1 open pipe handle(s) match a known C2/lateral-movement framework naming convention.,"An OS-confirmed open handle (not just a string sitting in memory) whose name matches a specific, narrow framework convention (e.g. Cobalt Strike's msagent_/postex_ or PsExec's psexesvc) is meaningfully unlikely to be coincidental — raised to HIGH only when further corroborated (see pipe.corroboration below).",Handle=0x88 ObjectName=\Device\NamedPipe\msagent_1337 framework=Cobalt Strike technique=SMB Beacon peer-to-peer pipe — default msagent pipe name mitre=T1090.001,Framework pipe-name conventions can be renamed/customized by an operator; absence of a match does not mean absence of C2.
pipe,pipe.string_scan_lead,lead,low,1 occurrence(s) of a '\pipe\' name found in non-system-DLL memory via byte-pattern scan.,"A string match proves only that these bytes exist somewhere in memory — not that any handle was ever opened by that name, nor that the name isn't leftover/freed heap data or an unrelated buffer. Reported as a lead for the analyst and to locate regions for C2-context/execution correlation; does NOT contribute to the pipe score on its own.",VA=0x1230101 file_offset=(not captured) name='\\.\\pipe\\msagent_1337' region_type=MEM_PRIVATE,No corresponding open handle found for this exact name.
pipe,pipe.corroboration,detection,high,1 open pipe handle(s) are corroborated by C2-style context and/or live RIP/EIP execution within ±4096 bytes of a matching pipe-name string's own address.,"Combines an OS-confirmed open handle with BOTH independent memory signals (C2 artifacts AND live execution), both within 4096 bytes of that pipe's name string — the strongest correlation this hunter can produce.",Handle=0x88 ObjectName=\Device\NamedPipe\msagent_1337 pipe_va=0x1230101  c2_va=0x1230116 c2_match='http://' distance=0x15 same_page=True  live_rip=TID:0x999@RIP=0x1230105 distance=0x4 same_page=True,"Name correlation between a handle's ObjectName and a string scan hit uses exact match on the canonicalized pipe name (prefix stripped, casefolded) — still not a cryptographic guarantee both refer to the identical kernel object if the process has multiple same-named pipe instances. | Proximity window is ±4096 bytes, not a proof of logical association — an unrelated string or thread could coincidentally fall within that window in a dense region."

