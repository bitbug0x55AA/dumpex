
══════════════════════════════════════════
  HUNT: Cobalt Strike Beacon Config
══════════════════════════════════════════

  [*] Scanning 2 segment(s) for beacon signature …
  [*] Scan complete.

  [!] Beacon config #1  ──────────────────────────────────────────────
  VA (process)               0x0000000000050100  ← virtual address in target process
  File offset (.dmp)         0x0000000000005100  ← byte offset inside .dmp file
  Region base (VA)           0x0000000000050000  ← for cross-referencing with --hunt injection
  Region size                0x226
  Region protect             PAGE_READWRITE
  XOR key                    0x69 'i'  (CS3 encoding)
  CS version (estimated)     3.x
  Context corroboration      none  — structural validity only

  cs_beacon.structural_config  [MEDIUM]  DETECTION
    Inference   : Structurally-valid Cobalt Strike beacon configuration (TLV wire format parsed,
                  known BeaconType, ASN.1-shaped public key) found at this address.
    Confidence  : medium — The config's own structural validity — TLV wire format, known field
                  types, a recognized BeaconType, and an ASN.1-shaped public key all lining up — is
                  itself hard to produce by chance, but no independent memory-context corroboration
                  (executable private region, or a thread executing within the same allocation) was
                  found for this hit.
    Facts:
      - VA=0x50100 file_offset=0x5100 xor_key=0x69 cs_version_estimated=3.x field_count=2
      - region=0x50000 size=0x226 protect=PAGE_READWRITE
    Limitations:
      - RIP/EIP-based execution corroboration could not fully run — ThreadListStream missing from
        this dump — a live-execution corroboration for this hit cannot be ruled out.

  ── C2 / Identity / Transport ──────────────────────────────────────
  BeaconType                 HTTP

  ── Full Config Field Table ────────────────────────────────────────
    Field       Type    Value
    BeaconType  uint16  0
    PublicKey   bytes   3014300d06092a864886f70d01010105000303003000

  [!] Beacon config #2  ──────────────────────────────────────────────
  VA (process)               0x0000000000060100  ← virtual address in target process
  File offset (.dmp)         0x0000000000006100  ← byte offset inside .dmp file
  Region base (VA)           0x0000000000060000  ← for cross-referencing with --hunt injection
  Region size                0x226
  Region protect             PAGE_READWRITE
  XOR key                    0x2E '.'  (CS4 encoding)
  CS version (estimated)     3.x
  Context corroboration      none  — structural validity only

  cs_beacon.structural_config  [MEDIUM]  DETECTION
    Inference   : Structurally-valid Cobalt Strike beacon configuration (TLV wire format parsed,
                  known BeaconType, ASN.1-shaped public key) found at this address.
    Confidence  : medium — The config's own structural validity — TLV wire format, known field
                  types, a recognized BeaconType, and an ASN.1-shaped public key all lining up — is
                  itself hard to produce by chance, but no independent memory-context corroboration
                  (executable private region, or a thread executing within the same allocation) was
                  found for this hit.
    Facts:
      - VA=0x60100 file_offset=0x6100 xor_key=0x2e cs_version_estimated=3.x field_count=2
      - region=0x60000 size=0x226 protect=PAGE_READWRITE
    Limitations:
      - RIP/EIP-based execution corroboration could not fully run — ThreadListStream missing from
        this dump — a live-execution corroboration for this hit cannot be ruled out.

  ── C2 / Identity / Transport ──────────────────────────────────────
  BeaconType                 HTTP

  ── Full Config Field Table ────────────────────────────────────────
    Field       Type    Value
    BeaconType  uint16  0
    PublicKey   bytes   3014300d06092a864886f70d01010105000303003000

  [ VERDICT ]  COBALT STRIKE — 2 beacon config(s) found in memory  (structural validity only — no independent memory-context corroboration)

