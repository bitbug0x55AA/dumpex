## hunt / summary
ttp,score,max_score,status,verdict,verdict_level,confidence,coverage_complete,coverage_reason,lead_count,review_priority
injection,3,3,DETECTED,HIGH,high,high,False,1 region(s) returned fewer bytes than requested while checking for hidden PE headers (short read) — not fully examined,3,high

## hunt / findings
ttp,finding_type,va_process,file_offset,cs_version,xor_key,beacon_type,c2_host,c2_uri,port,useragent,pipename,license_id,sleep_ms,jitter_pct,details
injection,injection_rwx_region,0x00007ff700000000,,,,,,,,,,,,,AllocationBase=0x7ff700000000; RegionSize=0x1000; Protect=PAGE_EXECUTE_READWRITE
injection,injection_hidden_pe_validated,0x00007ff700002000,,,,,,,,,,,,,AllocationBase=0x7ff700000000; machine=AMD64; sections=1
injection,injection_unbacked_thread_startaddr,0x00007ff700002000,,,,,,,,,,,,,TID=0x1
injection,injection_rip_in_suspicious_allocation,0x00007ff700002000,,,,,,,,,,,,,TID=0x1; RIP; AllocationBase=0x7ff700000000; full_correlation=True

## hunt / finding_details
ttp,check,tag,confidence,inference,rationale,facts,limitations
injection,injection.rwx_regions,lead,medium,"1 memory region(s) carry PAGE_EXECUTE_READWRITE/WRITECOPY protection, spanning 1 distinct allocation(s).","RWX is a directly-observed page protection flag, not a heuristic guess — but RWX alone is routinely used by JITs, debuggers, and some legitimate packers, so it is a lead, not proof, until corroborated by a validated PE header and/or live execution in the same allocation.",VA=0x7ff700000000 AllocationBase=0x7ff700000000 size=0x1000 type=MEM_PRIVATE protect=PAGE_EXECUTE_READWRITE,Does not by itself distinguish injection from JIT/legitimate self-modifying-code use cases.
injection,injection.hidden_pe_validated,lead,medium,"1 region(s) contain a structurally-valid PE header (DOS+COFF+optional header+full section table all parsed successfully) at an address absent from the module list, in MEM_PRIVATE memory, an executable unbacked mapping, or otherwise correlated with an RWX allocation or live thread execution.","Passing full structural PE validation (not just an 'MZ' prefix) rules out coincidental bytes and most decoys, but a valid header outside the module list can also occur from a manually-mapped but otherwise benign in-process library (e.g. some anti-cheat/DRM loaders) — confidence rises to HIGH only when a thread's live RIP/EIP actually executes inside the same allocation.",VA=0x7ff700002000 AllocationBase=0x7ff700000000 size=0x1000 type=MEM_PRIVATE protect=PAGE_READWRITE  |  PE header VALID: machine=AMD64 pe32plus=True sections=1 entrypoint_rva=0x1000 declared_image_base=0x140000000,Header validation read is capped at 4096 bytes; a section table extending past that reports as invalid rather than being partially trusted.
injection,injection.unbacked_thread_startaddress,lead,low,1 thread(s) began execution at an address not covered by any known module.,"StartAddress records where a thread BEGAN, not where it is executing now — a thread can legitimately start inside unbacked/JIT memory (e.g. a thread pool worker routine passed as a raw function pointer into private memory) and still be benign. Current RIP/EIP (below, when available) is the stronger signal for what a thread is actually doing.",TID=0x1 StartAddress=0x7ff700002000,
injection,injection.allocation_correlation,detection,high,"1 thread(s) currently execute inside an allocation that is simultaneously RWX and hosts a validated hidden PE — page type, structural PE validation, and live RIP/EIP all converge on the same AllocationBase.","This is the strongest evidence this hunter can produce: the process is, at the moment of the dump, actively running code from an allocation with no legitimate module backing and a concealed PE image.",TID=0x1 RIP=0x7ff700002000 -> VA=0x7ff700002000 AllocationBase=0x7ff700000000 size=0x1000 type=MEM_PRIVATE protect=PAGE_READWRITE  [FULL: RWX+validated-PE],RIP is a single-point-in-time snapshot; a thread that executed there moments before or after the dump was captured would not appear here.

